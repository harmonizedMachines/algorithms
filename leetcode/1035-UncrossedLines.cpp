class Solution {
public:
    int maxUncrossedLines(vector<int>& nums1, vector<int>& nums2) {
        int m = nums1.size(), n = num2.size();
        vector<int> dp();
        for (int i = 0; i < m; i++){
            for (int j = 0; j < n; j++){
                dp[] = ;
            }
        }
        return dp[-1];
    }
};